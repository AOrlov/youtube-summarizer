version: '3.8'

services:
  web:
    build: .
    ports:
      - "${PORT:-8081}:8080"
    volumes:
      - ./output:/app/output:rw
      - ./cache:/app/cache:rw
    restart: unless-stopped
    env_file:
      - stack.env
